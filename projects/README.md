# Projects in this repository

| Folder | Description |
|--------|-------------|
| [`local-llm-evaluation/`](local-llm-evaluation/) | **CGU IST 345** – compare multiple on-device LLMs (Dify testbed, diabetes-focused eval). Start at `local-llm-evaluation/README.md`. |

The main **Flutter** application remains at the repository root (`lib/`, `pubspec.yaml`).
